

<%= form_for [@tweets] do |f| %>
         
          
          <div style="margin: 8px 0">
            <%= f.text_area :text, placeholder: '編集してね！', style: 'width: 100%;height: 300px;' %>
          </div>
          <div class="row">
            <div class="col10 push1">
              <%= button_tag type: "submit", class: "btn btn--block" do %>
              編集内容を保存する<i class="icon-arrow-right"></i>
              <% end %>
            </div>
          </div>
          <% end %>